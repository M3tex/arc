VAR toto, titi <- 4
VAR tutu

PROGRAMME()
VAR tutu, x
DEBUT
    # Test des expressions arithmétiques sans variables
    /* Également un commentaire (et // aussi) */
    5 * (3 + 1) / (10 - 8)
    toto <- titi * 2 + x
    # NON 10
FIN 
