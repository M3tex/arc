/* Fonctionnalités arithmétiques de base */
PROGRAMME()
VAR x, y, z
DEBUT
    x <- 2
    y <- 2 + 3 * -4 - - 1
    z <- x * y + 1

    /* La bande de sortie doit-être [2, -9, -17] */
    ECRIRE(x)
    ECRIRE(y)
    ECRIRE(z)
FIN