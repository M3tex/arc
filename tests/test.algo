ALGO factorielle(n)
VAR res <- 1
DEBUT
    SI n > 1 ALORS
        res <- n * factorielle(n - 1)
    FSI
    RETOURNER res
FIN


PROGRAMME()
VAR n, i
DEBUT
    n <- LIRE()
    POUR i DANS 1...n + 1 FAIRE
        ECRIRE(factorielle(i))
    FPOUR
FIN