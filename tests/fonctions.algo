ALGO foo(a, b)
DEBUT
    RETOURNER a * b + 2
FIN


ALGO bar(a, b)
VAR x, y
DEBUT
    x <- foo(a, b)
    RETOURNER x
FIN


VAR toto <- 42

PROGRAMME()
VAR x <- 1, y <- 4
DEBUT
    ECRIRE(bar(x, y))
FIN
